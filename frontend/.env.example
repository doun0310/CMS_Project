# Keep synthetic SSE notifications disabled unless explicitly demonstrating the UI.
VITE_ENABLE_DEMO_EVENTS=false

# Vite forwards /api requests to the Express server at this address.
VITE_API_PROXY_TARGET=http://127.0.0.1:3000
