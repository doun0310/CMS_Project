'main' 브랜치 이름 바꾸기
git branch -m main-old

깃허브에서 커밋 가져오기
git fetch origin

깃허브에서 새로운 `main` 브랜치 만들기
git checkout --track origin/main

DB 실행 명령어
npm run db:setup

FormEvent는 SubmitEvent<HTMLFormElement>로 변경

초기 로딩속도 개선이 필요할 시에 Suspense나 Lazy를 사용 (IF/ELSE 코드를 사용하지 않고도 선언적으로 상태 관리 가능)

[vite] https proxy error: 가 지속적으로 발생 시에 vite.config.ts 파일 확인

* 백엔드 서버 연결 및 데이터베이스 필요

* 데이터셋 검토 후 적용하기
    1. bc-pii-ko (CC-BY-4.0, 원본까지 출처 표시 필요) [조건부 사용가능]
    2. DocLayNet (CDLA-Permissive-1.0) [비교적 안전]
    3. synthetic_pii_finanace_multilingual 
    4. OpenPII 1M (CC-BY-4.0, 상업적 이용 허용) [조건부 사용가능]

    * PII 탐지: bc-pii-ko + OpenPII 1M + 자체 한국어 검증 데이터
    * 금융 문서: Gretel 금융 PII 데이터 + 자체 재무·회계 양식
    * OCR·레이아웃: DocLayNet + CORD-v2 + 실제 사내 스캔 문서
    * 보안등급·승인 예측: 공개 데이터보다 자체 익명화 데이터 중심
    * 프린터 예측 유지보수: 운영 과정에서 SNMP 시계열 데이터를 직접 축적

    * 사용 전 THIRD_PARTY_DATASETS.md 작성 
    * 데이터셋 커밋-푸시 시 commit SHA로 고정
    * 파일 체크섬 저장 및 라이선스 파일 원본 보관
    * 학습 전 실존 개인정보 확인